<%
  colors = @family.theme_colors
  progress = @completion.progress_percentage
%>

<div class="max-w-3xl mx-auto">
  <!-- Header -->
  <div class="mb-6">
    <div class="flex items-center justify-between mb-2">
      <h2 class="text-2xl font-bold theme-text"><%= @rhythm.name %></h2>
      <span class="text-sm font-medium" style="color: <%= colors[:primary] %>;">
        <%= @completion.checked_count %>/<%= @completion.total_count %> complete
      </span>
    </div>

    <!-- Progress Bar -->
    <div class="w-full bg-gray-200 rounded-full h-3">
      <div class="h-3 rounded-full transition-all duration-300" style="width: <%= progress %>%; background-color: <%= colors[:primary] %>;"></div>
    </div>
  </div>

  <!-- Checklist -->
  <div class="bg-white rounded-lg shadow border theme-border overflow-hidden">
    <% @completion_items.each_with_index do |item, index| %>
      <div id="completion_item_<%= item.id %>" class="<%= 'border-t theme-border' if index > 0 %>">
        <%= render "completion_item", item: item, family: @family %>
      </div>
    <% end %>
  </div>

  <!-- Actions -->
  <div class="mt-8 flex flex-wrap items-center gap-4">
    <% if @completion.all_items_checked? %>
      <%= button_to "Complete Meeting", finish_family_rhythm_path(@family, @rhythm),
          method: :post,
          class: "theme-button-filled text-lg px-6 py-3" %>
    <% else %>
      <button disabled class="px-6 py-3 rounded font-medium text-white opacity-50 cursor-not-allowed" style="background-color: <%= colors[:primary] %>;">
        Complete Meeting
      </button>
      <span class="text-sm text-gray-500">Check all items to complete</span>
    <% end %>

    <%= link_to "Save & Exit", family_rhythm_path(@family, @rhythm),
        class: "theme-button-outline" %>
  </div>

  <!-- Tips -->
  <div class="mt-8 p-4 bg-gray-50 rounded-lg text-sm text-gray-600">
    <strong>Tips:</strong>
    <ul class="list-disc list-inside mt-2 space-y-1">
      <li>Click each item to mark it complete</li>
      <li>Use the link buttons to open related sections in a new tab</li>
      <li>Your progress is saved automatically - you can leave and come back</li>
    </ul>
  </div>
</div>
